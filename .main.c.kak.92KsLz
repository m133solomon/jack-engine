#include "src/jack.h"

void loop(float delta_time);

int main()
{
    if (!jack_init()) return -1;

    // init here

    jack_loop_func(loop);
    return 0;
}

void loop(float delta_time)
{
    jack_clear(J_GRAY);
}
